using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Categorizes each status to perform a specific action when applied to a character
public enum StatusType {

    damageOverTime = 0,
    slow = 1,
    inaction = 2,
    boost = 3

}