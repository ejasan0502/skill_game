using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Categorizes each skill gem that will perform specific roles when applied to a skill
public enum SkillGemCategory {

    element = 0,
    aoe = 1,
    effect = 2
    
}